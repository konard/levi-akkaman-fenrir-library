node_modules/
.env
.env.local
npm-debug.log*
*.log
coverage/

# Uploaded user avatars (runtime data, keep the folder via .gitkeep)
public/uploads/*
!public/uploads/.gitkeep

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Playwright MCP scratch output and working screenshots (kept out of git).
# Only root-level PNGs are scratch; committed screenshots live in docs/.
.playwright-mcp/
/*.png
